# NexusOS AI environment configuration

APP_ENV=development
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_URL=http://localhost:3000

# Google Gemini API key (optional — heuristic fallbacks used when empty)
AI_PROVIDER=gemini
AI_API_KEY=

# Frontend API base URL (used at build time for Docker)
VITE_API_URL=http://localhost:8000
